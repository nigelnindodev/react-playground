import { useState } from 'react';

function App() {

  return (
    <div>
      <h1>Counter</h1>
      <p>Count: 0</p>
      <button onClick={() => { }}>+</button>
      <button onClick={() => { }}>-</button>
    </div>
  );
}

export default App;
